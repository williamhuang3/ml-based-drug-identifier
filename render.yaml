services:
  - type: web
    name: drugpredict-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python flask_app.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: ALLOWED_ORIGINS
        value: https://drugpredict.vercel.app
    autoDeploy: true
